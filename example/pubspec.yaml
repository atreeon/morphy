name: example

environment:
  sdk: ">=3.8.0 <4.0.0"

dependency_overrides:
  morphy_annotation:
    path: ../morphy_annotation

dependencies:
  morphy_annotation:
    path: ../morphy_annotation

dev_dependencies:
  morphy:
    path: ../morphy
  build_runner: ^2.12.2
  test: ^1.30.0
  json_serializable: ^6.11.2
  json_annotation: ^4.9.0

