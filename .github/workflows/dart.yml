name: Dart CI

on:
  pull_request:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - uses: dart-lang/setup-dart@v1
        with:
          sdk: "3.8.0"  # pick the exact version you want

#      - name: Provide frontend_server snapshot for Dart 3.8.x
#        run: |
#          set -e
#          echo "DART_HOME: $DART_HOME"
#          # Grab a small Flutter to borrow the snapshot from (linux-x64)
#          curl -L https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.3-stable.tar.xz | tar -xJ
#          mkdir -p "$DART_HOME/bin/snapshots"
#          ln -sf "$PWD/flutter/bin/cache/artifacts/engine/linux-x64/frontend_server.dart.snapshot" \
#              "$DART_HOME/bin/snapshots/frontend_server.dart.snapshot"
#          ls -l "$DART_HOME/bin/snapshots"

      - run: |
          dart --version
          echo "DART_HOME: $DART_HOME"
          ls -l "$DART_HOME/bin/snapshots" || true

#      - uses: dart-lang/setup-dart@v1
#        with:
#          sdk: "3.8.2"
##          sdk: stable

      - name: Setup annotation
        working-directory: morphy_annotation
        run: dart pub get

#      - name: Clean stale tool state
#        run: rm -rf .dart_tool/build .dart_tool/test .dart_tool/package_config.json
#
      - name: Morphy get
        working-directory: morphy
        run: dart pub get

      - name: Morphy test
        working-directory: morphy
        run: dart test

      - name: Setup example
        working-directory: example
        run: |
          dart pub get
          dart run build_runner build --delete-conflicting-outputs

      - name: Run tests Example
        working-directory: example
        run: dart test
